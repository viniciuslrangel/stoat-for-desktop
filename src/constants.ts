export const sinkName = "stoat-virtual-sink";
export const sourceName = "stoat-virtual-source";

/** Default remote web client URL when no custom server is configured. */
export const DEFAULT_SERVER_URL = "https://stoat.chat/app";
